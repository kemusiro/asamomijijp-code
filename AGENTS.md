# AGENTS.md

## リポジトリの目的

このリポジトリは、`asamomiji.jp`の記事に付随するソースコードを公開する。対象には、当初Gitで管理されていなかった歴史的プログラムも含む。

Webサイトと本リポジトリは疎結合に保つ。Astroサイトのビルドを、本リポジトリ、Git submodule、ネットワークからのダウンロードに依存させない。

## リポジトリ構成

- 記事に付随する完全なプロジェクトは`projects/<article-slug>/`へ配置する。
- 各プロジェクトに`README.md`を置き、対応記事URL、目的、対応環境または歴史的環境、来歴、検証状態、制約を記載する。
- 短い説明用コードは、記事内に置いた方が分かりやすい場合は記事へ掲載し、完全なプロジェクトへリンクする。
- 独立したリリース周期、Issue管理、貢献者コミュニティ、または相当な規模を持つようになったプロジェクトだけを別リポジトリへ分離する。

## 歴史的ソースコードの方針

- Gitの履歴を捏造したり、コミット日時を過去へ変更したりしない。原作成日または原公開日はプロジェクトのREADMEへ記録し、取り込み時点の実際の日付でコミットする。
- 原資料アーカイブ名、SHA-256、機械的変換の内容を記録し、ソースの来歴を保持する。
- 原資料アーカイブはGit管理外で保存する。読者にとって有用であり、所有者が承認した場合に限り、検査済みアーカイブをGitHub Releaseの添付ファイルとして公開する。
- 必要な場合は、Git公開用のソーステキストをUTF-8へ変換する。文字コード変換だけの取り込みではプログラムの動作を変更せず、可能な範囲で往復変換または同等のハッシュ検査を行う。
- 現行ツールチェーンでコンパイルできるという理由だけで、歴史的コードを現在もサポートされているものとして説明しない。

## ライセンスと第三者要素

- リポジトリ所有者が作成・公開するコードには、プロジェクト側で別のライセンスを明示しない限り、ルートの`LICENSE`にあるBSD-2-Clauseを適用する。
- ソースファイル内の著作権、著作者、ライセンス、帰属表示を保持する。
- 第三者のコード、データ、メディア、フォント、文書を、その利用条件で認められていない限り、本リポジトリのライセンス対象として配置しない。
- プロジェクト固有の例外や第三者要素は、そのプロジェクトの`README.md`または`THIRD_PARTY_NOTICES.md`へ記録する。
- 所有権、秘密性、個人情報、再配布権が未確認のものは公開しない。

## 公開時の安全性

- 認証情報、トークン、秘密鍵、データベースダンプ、サーバー設定の秘密情報、個人用バックアップ、`.env`ファイルをコミットしない。
- インポートしたアーカイブを展開・公開する前に、絶対パス、`..`によるパストラバーサル、シンボリックリンク、想定外のバイナリ、秘密情報らしき内容を検査する。
- Gitではソースを優先する。生成したクラス、JAR、ファームウェアイメージ、インストーラー、その他のバイナリは、記録された理由がない限り通常のGit履歴へ含めず、承認済みのGitHub Releaseを使用する。
- 未確認の歴史的バイナリを実行しない。静的検査や隔離環境でのコンパイルは、実行時の安全性を意味しない。

## 検証と文書化

- ビルド手順とテスト手順は各プロジェクト内に記載し、互いに無関係な歴史的プロジェクトへ単一のビルドシステムを強制しない。
- 使用したツールチェーンの正確なバージョンと、検査が静的検査、コンパイル、自動テスト、エミュレーター試験、実機試験のいずれであるかを記録する。
- 記事を公開するときは、レビュー済みプロジェクトの状態を示す変更不能なタグまたはコミットへリンクする。デフォルトブランチへの案内用リンクは別途追加してよい。
- 変更を引き渡す前に、対象プロジェクトの検査と`git diff --check`を実行する。

## 変更管理

- 所有者の明示承認なしに、コミット、push、Release作成、リポジトリ設定変更、バイナリ公開を行わない。
- 明示承認なしに、公開済み履歴の書き換えやReleaseの削除を行わない。
- 無関係なファイルを変更せず、作業ツリーにある所有者の変更を保持する。
